# Rendered output / packaged charts
*.tgz
rendered/

# Chart dependencies are vendored, so ArgoCD never has to download them.
!charts/*/charts/*.tgz

# IDE
.idea/

# Local agent guidance
CLAUDE.md
# Terraform (state holds cluster credentials, never commit)
terraform*/.terraform/
terraform*/.env
*.tfstate
*.tfstate.*
*.tfvars
crash.log
/docs
